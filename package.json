{
  "name": "auralis",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "netlify deploy --prod --build"
  },
  "devDependencies": {
    "netlify-cli": "latest",
    "vite": "latest"
  }
}
